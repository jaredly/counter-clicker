# Changesets

Run `npm run changeset` to describe user-facing changes. Merging the generated release PR bumps `package.json`, and the release workflow builds and attaches a debug APK to a GitHub Release.
